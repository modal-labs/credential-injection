[project]
name = "credential_injection_demo"
version = "0.1.0"
description = "A demo of credential injection with a Modal sandbox"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "modal>=1.4.2",
    "pyjwt>=2.12.1",
    "starlette>=1.0.0",
]
